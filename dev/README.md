# Development

## How to release

```bash
git clone git@github.com:enactic/dora-openarm-actions-executor.git
cd dora-openarm-actions-executor
dev/release.sh ${VERSION} # e.g. dev/release.sh 1.0.0
```
